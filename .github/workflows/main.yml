on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: Run react build script
        run: npm install
          npm run build
        env:
          CI: false

      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp://ftp.solarich.com.ph
          username: deploy@solarich.com.ph
          password: ${{ secrets.FTP_PASSWORD_SOLARICH }}
          local-dir: build/
